<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.listenwithus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListenWithUs">
        <activity android:name=".firstpage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".singin"/>
        <activity android:name=".scientific" />
        <activity android:name=".history" />
        <activity android:name=".Math" />
        <activity android:name=".buttons" />
        <activity android:name=".religious" />
         <activity android:name=".Raview"></activity>

    </application>

</manifest>